package Game{	import flash.display.Sprite;	import flash.display.Bitmap;	import flash.display.BitmapData;	import flash.events.Event;	import flash.events.MouseEvent;		public final class Pause extends BaseScreen	{		public var w:Game.World;				public function Pause(_w:Game.World, _image:BitmapData)		{						super(_image, initializeComplete);						w = _w;			w.visible = false;			}		private function initializeComplete():void		{			resumeButton.label.text = "RESUME";			resumeButton.addEventListener(MouseEvent.CLICK, resume, false, 0, true);						quitButton.label.text = "QUIT";			quitButton.addEventListener(MouseEvent.CLICK, quit, false, 0, true);		}		private function resume(e:MouseEvent):void		{			close(transitionOutComplete);		}				private function transitionOutComplete():void		{			w.pause = false;			w.visible = true;		}				private function quit(e:MouseEvent):void		{						w.dispose();			parent.addChild(new Menu(Screenshot.create()));			parent.removeChild(this);		}	}}